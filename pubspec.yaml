name: ia_financeiro
description: IA Financeiro - App de finanças pessoais com IA
publish_to: 'none'
version: 1.0.13+14

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.0
  cloud_functions: ^5.0.0
  google_sign_in: ^6.2.1
  url_launcher: ^6.3.0
  http: ^1.2.0
  intl: ^0.19.0
  in_app_purchase: ^3.2.0
  firebase_storage: ^12.1.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.1
  cached_network_image: ^3.4.1
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
